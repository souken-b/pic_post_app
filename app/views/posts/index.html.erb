<%= link_to  "投稿", new_post_path %>
<ul>
<% @posts.each do |post| %>
  <% if current_user.id == post.user_id %>
    <li><%= link_to post.message, edit_post_path(post) %></li>
  <% else %>
    <li><%= post.message %></li>
  <% end %>
<% end %>
</ul>