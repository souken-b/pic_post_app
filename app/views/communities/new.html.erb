<div class="community">
<h1>コミュニティの作成</h1>
  <%= form_with model: @community, local: true do |f| %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.text_field :name %>
    <%= f.submit %>
  <% end %>
</div>